apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartlock-frontend.fullname" . }}
  labels:
    {{- include "smartlock-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartlock-frontend.selectorLabels" . | nindent 4 }}
